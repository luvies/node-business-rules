{
  "name": "node-business-rules",
  "version": "0.0.1",
  "description": "A JS-expression-based rules engine",
  "main": "dist/index.js",
  "repository": "https://github.com/luvies/node-business-rules",
  "author": "luvies",
  "license": "MIT",
  "scripts": {
    "build": "tsc --project .",
    "lint": "tslint --project . && prettier --check 'src/**/*.ts' 'test/**/*.ts'",
    "fix": "tslint --project . --fix && prettier --check --write 'src/**/*.ts' 'test/**/*.ts'",
    "test": "mocha -r ts-node/register test/**/*.ts",
    "postpublish": "git push && git push --tags"
  },
  "dependencies": {
    "jsep": "^0.3.4"
  },
  "devDependencies": {
    "@types/chai": "^4.1.7",
    "@types/mocha": "^5.2.6",
    "@types/node": "^11.11.3",
    "chai": "^4.2.0",
    "mocha": "^6.0.2",
    "prettier": "^1.16.4",
    "ts-node": "^8.0.3",
    "tslint": "^5.14.0",
    "tslint-config-prettier": "^1.18.0",
    "tslint-eslint-rules": "^5.4.0",
    "typescript": "^3.3.3333"
  }
}
